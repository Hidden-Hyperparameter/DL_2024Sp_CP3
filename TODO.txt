GAN: 
2,0,1,3 or 2,1,0,3 -- same
activation in linear && size
add auto-adjustment, if too high fake acc just train D on real dataset
discriminator stride first layer can't too large
too much noise -> reduce B 
